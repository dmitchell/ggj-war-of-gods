/* global cloak */

cloak.configure({
  messages: {
    init: function(msg) {

    }
  }
});

cloak.run('http://localhost:8090');

// map.addEventListener('submit', function(e) {
//   cloak.message('chat', msg);
// });
